{
  "name": "mardawaid",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf:build": "astro build",
    "pages:build": "astro build",
    "pages:deploy": "astro build && wrangler pages deploy dist --project-name=mardawaid --commit-dirty=true",
    "pages:deploy:preview": "astro build && wrangler pages deploy dist --project-name=mardawaid --branch=preview --commit-dirty=true"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.1.3",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "wrangler": "^4.28.0"
  }
}
